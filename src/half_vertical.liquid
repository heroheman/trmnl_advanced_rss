{% if status == 'error' %}
<div class="layout layout--col">
  <span class=title>
    {{ message }}
  </span>
  <span class=description>
    {{ trmnl.plugin_settings.polling_url | json }}
  </span>
</div>
{% else %}


<div class="layout layout--no-bleed">
  <div class="grid">

    <div class="col col--start col--span-3">
      <div class="p--2">

        <div class="list" data-list-limit="true" data-list-max-height="370" data-list-hidden-count="false" data-list-max-columns="1">
          {% for item in items limit:6 %}
          {% 
          render "media_item", 
          title: item.title, 
          image: item.media_thumbnail_url,
          description: item.description,
          body: item.content_encoded
          show_thumb: trmnl.plugin_settings.custom_fields_values.show_images_medium
          show_description: trmnl.plugin_settings.custom_fields_values.show_description_medium
          %}

          {% endfor %}

        </div>

      </div>
    </div>
  </div>
</div>

{% endif %}

{% if  trmnl.plugin_settings.custom_fields_values.show_title_bar == true %}
{% render "title_bar" image: feed.image, title: title, description: description %}
{% endif %}